# Thermometer
This repository contains a Verilog HDL implementation of a Digital Thermometer. The system reads temperature data from a hardware sensor, processes the digital signal through a finite state machine (FSM), and converts the raw binary data into a human-readable format for display.
